; R1 golden-diff check target. Run from the repo root:
;   dune exec test/golden/golden_diff.exe -- check     (CI gate)
;   dune exec test/golden/golden_diff.exe -- update    (re-bless goldens)
;   dune exec test/golden/golden_diff.exe -- verify    (determinism gate)
;   dune exec test/golden/golden_diff.exe -- check-order (iteration-order gate)

(executable
 (name golden_diff)
 (modules golden_diff)
 (flags
  (:standard -g -w -33-26))
 (libraries mordor_lib lwt lwt.unix z3 zarith core str unix)
 (preprocess
  (pps lwt_ppx)))

; S8 (#20) loop goldens: the structure symbolic loop semantics builds.
;   dune exec test/golden/loop_goldens.exe -- check
;   dune exec test/golden/loop_goldens.exe -- update FILE ...

(executable
 (name loop_goldens)
 (modules loop_goldens)
 (libraries mordor_lib lwt lwt.unix unix))
