(library
 (name smrd_lib)
 (wrapped false)
 (modules
  coherence
  elaborations
  events
  executions
  expr
  forwardingcontext
  interpret
  justifications
  parse
  rewrite
  solver
  symmrd
  trees
  types
  uset
  utils)
 (flags
  (:standard -w -26-27))
 (libraries core lwt lwt.unix z3 zarith landmarks)
 (preprocess
  (pps lwt_ppx landmarks-ppx))
 (public_name smrd))

(executable
 (name main)
 (public_name smrd)
 (modules main)
 (libraries smrd_lib lwt lwt.unix landmarks)
 (preprocess
  (pps lwt_ppx landmarks-ppx)))
