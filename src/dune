(ocamllex lexer)

(menhir
 (modules parser)
 (flags --explain))

(library
 (name mordor_lib)
 (wrapped false)
 (modules
  ast
  coherence
  elaborations
  events
  executions
  expr
  eventstructureviz
  forwardingcontext
  interpret
  justifications
  lexer
  parse
  parser
  rewrite
  solver
  symmrd
  trees
  types
  uset
  utils)
 (flags
  (:standard -g -w -26-27))
 (libraries core ocamlgraph lwt lwt.unix z3 zarith)
 (public_name smrd)
 (preprocess
  (pps lwt_ppx)))

(executable
 (name main)
 (public_name mordor)
 (modules main)
 (flags
  (:standard -g))
 (libraries mordor_lib lwt lwt.unix)
 (preprocess
  (pps lwt_ppx)))
