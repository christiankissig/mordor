name=spinlock 1
%%
// The lock, a global: 0 = unlocked, 1 = locked
mutex := 0;

// --- acquire ---
do {
  rold := cas(acquire, relaxed, mutex, 0, 1);
} while (rold = 0);

// --- critical section ---
// (protected work goes here)

// --- release ---
mutex := 0;
