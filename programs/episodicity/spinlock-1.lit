name=spinlock 1
%%
// The lock, a global: 0 = unlocked, 1 = locked
mutex := 0;
rpmutex := &mutex;

// --- acquire ---
do {
  rold := cas(acquire, relaxed, rpmutex, 0, 1);
} while (rold = 0);

// --- critical section ---
// (protected work goes here)

// --- release ---
mutex := 0;
